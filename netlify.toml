[build]
publish = "site/public"
command = "hugo -s site"

[context.production.environment]
HUGO_VERSION = "0.66"
HUGO_ENV = "production"
HUGO_ENABLEGITINFO = "true"

[context.split1]
command = "hugo --enableGitInfo -s site"

[context.split1.environment]
HUGO_VERSION = "0.66"
HUGO_ENV = "production"

[context.branch-deploy]
command = "hugo -b $DEPLOY_PRIME_URL -s site"

[context.branch-deploy.environment]
HUGO_VERSION = "0.66"
