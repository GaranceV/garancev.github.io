{
  "name": "garancev.github.io",
  "version": "1.1.0",
  "description": "garance's personal blog",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "http-server -c-1 dist/",
    "assets": "cp -R src/assets dist/ && cp src/favicon.ico dist/",
    "pwa": "cp src/manifest.json dist/ && cp src/js/sw.js dist/ && npm run miniJs:sw",
    "js": "npm run js:sw",
    "js:dev": "npm run js:sw:dev",
    "js:sw": "npm run js:sw:dev && npm run miniJs:registersw",
    "js:sw:dev": "cp src/js/registersw.js dist/js/",
    "miniJs": "npm run miniJs:registersw && npm run miniJs:sw",
    "miniJs:registersw": "node_modules/uglify-js/bin/uglifyjs dist/js/registersw.js -o dist/js/registersw.js -c -m",
    "miniJs:sw": "node_modules/uglify-js/bin/uglifyjs dist/sw.js -o dist/sw.js -c -m",
    "clean": "rm -rf dist && mkdir dist && mkdir dist/js && mkdir dist/css",
    "watch:hbs": "onchange 'src/templates/*.hbs' 'src/pages/*.hbs' 'src/index.hbs' -- npm run handlebars",
    "watch:assets": "onchange 'src/assets/*.*' -- npm run assets",
    "watch:js": "onchange 'src/js/registersw.js' -- npm run js:sw && onchange 'src/js/sw.js' -- npm run pwa",
    "watch": "npm run watch:hbs & npm run watch:assets",
    "css": "node build/postcss.js",
    "handlebars": "node build/handlebars.js",
    "html:mini": "node build/miniHtml.js",
    "html": "npm run handlebars && npm run html:mini",
    "build": "npm run clean && cp CNAME dist/ && npm run html && npm run css && npm run assets && npm run js && npm run pwa",
    "build:dev": "npm run clean && npm run handlebars && npm run css && npm run assets && npm run js:dev && npm run pwa"
  },
  "repository": {
    "type": "git",
    "url": "git@me.github.com:garancev/garancev.github.io.git"
  },
  "keywords": [
    "blog"
  ],
  "author": "Garance Flore Vallat",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^3.10.0",
    "handlebars": "^4.0.11",
    "html-minifier": "^3.5.10",
    "onchange": "^3.3.0",
    "postcss": "^6.0.17",
    "postcss-cli": "^5.0.0",
    "postcss-import": "^11.1.0",
    "uglify-js": "^3.3.14"
  }
}
